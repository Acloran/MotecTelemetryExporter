{
  "name": "motec-telemetry-exporter-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "backend": "uvicorn motec_exporter.app:app --reload --app-dir backend/src --port 8010",
    "frontend": "npm --prefix frontend run dev -- --host 0.0.0.0 --port 5174",
    "simulator": "npm --prefix simulator run dev -- --host 0.0.0.0 --port 5175",
    "test": "pytest",
    "lint": "python -m compileall backend/src"
  }
}
